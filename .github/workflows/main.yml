name: test-CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
env:
  post_id: 155
  
jobs:
  update_wp_post:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: install pip markdown
        run: |
          pip install markdown2

      - name: convert md 2 markdown
        id: makeHTML
        run: |
          text=$(python3 -m markdown2 ./README.md)
          echo "::set-output name=contentHTML::$(echo $text)"
          echo $text

      - name: post
        run: |
         curl -X POST -Iu ${{ secrets.WP_USER }}:${{ secrets.WP_APP_PW }}  \ 
         -H "Content-Type: application/json" \
         -d '{ "content":${{ steps.makeHTML.outputs.contentHTML }}, "status" : "pending" }' \
         https://swsd.net/wp-json/wp/v2/posts/${{env.post_id}}

      - name: makedata
        id: postData
        run: |
          #titleText = ""
          echo "hello"
      - name: post
        id: postInfo
        uses: fjogeleit/http-request-action@v1
        with:
          url: 'https://swsd.net/wp-json/wp/v2/posts/155'
          method: 'POST'
          username: ${{ secrets.AWX_USER }}
          password: ${{ secrets.AWX_PASSWORD }}
          customHeaders: '{"Content-Type": "application/json"}'
          data: '{"content" : ${{ steps.getcontentHTML.outputs.contentHTML }} , "status" : "pending" }'
          
      - name: Show Response
        run: |
          echo ${{ steps.postInfo.outputs.response }}
          echo ${{ steps.postInfo.outputs.headers }}
